require('dotenv').config();
const express = require('express');

// Sets EJS as the view engine

// Adds some logging to each request

// Routes

// FAVE Routes
// Get /faves -- READ all faves and dispaly them to user

// POST /faves -- CREATE new face and redirect to /faves to display user faves
